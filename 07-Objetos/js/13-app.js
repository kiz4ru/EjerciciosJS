const producto = {
    nombre: "Monitor 20 Pulgadas",
    precio: 300,
    disponible: true
}

//Object, keys, values, entries
console.log(Object.keys(producto)); //retorna las llaves del objeto
console.log(Object.values(producto)); //retorna los valores del objeto
console.log(Object.entries(producto)); //retorna las llaves y valores del objeto    